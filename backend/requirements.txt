# ============================================
# Banking App — Python Packages
# ============================================
# Install with: pip install -r requirements.txt
#
# OPTIONAL extras (only install if you need them):
#   PostgreSQL : pip install -r requirements-postgres.txt
#   Redis cache: pip install -r requirements-redis.txt

# --- Django Core ---
Django==4.2.7                        # The main web framework
djangorestframework==3.14.0          # Adds REST API support to Django
djangorestframework-simplejwt==5.3.0 # JWT login tokens (like digital ID cards)
setuptools<81                        # Required so pkg_resources still works with simplejwt 5.3.0
django-cors-headers==4.3.0           # Allows React (port 5173) to talk to Django (port 8000)

# --- Environment Variables ---
python-decouple==3.8                 # Read .env file safely (keeps secrets out of code)

# --- Optional: PostgreSQL (only needed if USE_POSTGRESQL=True in .env) ---
# Install separately: pip install psycopg2-binary==2.9.9
# Requires PostgreSQL to be installed on the system.

# --- Optional: Redis Cache (only needed if USE_REDIS=True in .env) ---
# Install separately: pip install redis==5.0.1 django-redis==5.4.0
